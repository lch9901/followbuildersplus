{
  "name": "followbuildersplus-scripts",
  "version": "1.0.0",
  "description": "Scripts for Follow Builders Plus — feed generation, digest preparation, archive lookup, delivery",
  "type": "module",
  "scripts": {
    "generate-feed": "node generate-feed.js",
    "prepare-digest": "node prepare-digest.js",
    "lookup-detail": "node lookup-detail.js"
  },
  "dependencies": {
    "dotenv": "^16.4.0",
    "proper-lockfile": "^4.1.0"
  }
}
